liteflow:
  rule-source: workflow/coze/flow.el.xml
  ai:
    base-packages:
      - com.yomahub.liteflow.test.ai.workflow.coze.cmp
    enable: true
    workflow:
      coze:
        apiKey: mock-api-key # 测试代码不需要配置，将使用 mock 数据，不会调用 AI 服务接口
